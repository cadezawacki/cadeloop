# Pre-built wheels (in-repo snapshot)

Committed on request as a convenience snapshot — canonical distribution
is the `release.yml` pipeline (PGO + `-v3` variants on tag push), and
these files are NOT that: no PGO, plain `--release` builds of the
commit they ship with.

| wheel | built | verified |
|---|---|---|
| `cadeloop-0.0.1-cp311-cp311-manylinux_2_34_x86_64.whl` | natively on Linux (glibc ≥ 2.34) | ✅ installed into a fresh venv; import + full bisect battery passed |
| `cadeloop-0.0.1-cp311-cp311-win_amd64.whl` | **cross-compiled from Linux** (`x86_64-pc-windows-gnu`, mingw-w64 GCC 13; `python311.dll` import library generated by pyo3's `generate-import-lib`) | ✅ an earlier build of this wheel passed the full IOCP `rio_smoke` battery on Windows 11 hardware; ⚠️ RIO data path under diagnosis there (see below) |

Windows caveats, stated plainly:

- Prior to this wheel, every Windows-validated build was compiled
  **natively with MSVC**. The mingw-GNU build has since executed on
  real hardware: the complete IOCP battery (echo, 120-conn storm,
  native HTTP, abrupt-close, soak) passed, which also settles the CRT
  question below in practice.
- It links the legacy `msvcrt.dll` CRT (Debian's mingw-w64 win32
  flavor), not the UCRT that CPython itself uses. The extension passes
  no CRT resources (FILE*, malloc'd memory, fds) across that boundary.
- If it misbehaves, build natively instead — on a Windows machine with
  Rust + Python 3.11: `pip install maturin && maturin build --release`
  (or take a wheel from CI / `release.yml`).

Install directly:

```
pip install wheels/cadeloop-0.0.1-cp311-cp311-win_amd64.whl      # Windows
pip install wheels/cadeloop-0.0.1-cp311-cp311-manylinux_2_34_x86_64.whl  # Linux
```

## Checking RIO on a machine without Rust

The wheel contains the complete RIO backend — Rust is a build-time
dependency only. On any Windows box with Python 3.11:

```
pip install cadeloop-0.0.1-cp311-cp311-win_amd64.whl

:: 1. does RIO initialize? (the exact call that failed on the beta box)
python -c "from cadeloop.loop import Loop; lp = Loop(backend='rio'); print('RIO OK:', lp.stats()['backend']); lp.close()"

:: 2. full behavioral battery, iocp first (proves the wheel), then rio:
python tools\windows\rio_smoke.py iocp rio --out rio-results.json

:: 3. if RIO checks stall or fail: the ~30s bisector localizes which
::    machinery is at fault and prints the diagnosis key:
python tools\windows\rio_bisect.py rio --out rio-bisect.json

:: 4. the entire test suite on RIO (pip install pytest; repo clone, no build):
set CADELOOP_BACKEND=rio
python -m pytest tests\unit tests\conformance
```

Step 1 failing prints the named failing RIO call (e.g.
`RIOCreateCompletionQueue(IOCP-notify)`); the Rust-built `rio_probe`
diagnostic is only ever needed to dig further into a failure.

`stats()` on a RIO loop reports two diagnosis counters:
`rio_notifies` (RIONotify completion packets received) and
`rio_watchdog_reaps` (completions the poll-top drain found while a
notification was armed). A healthy machine shows notifies > 0 and
reaps ≈ 0; reaps piling up means the CQ works but notification
delivery does not — the loop then stays live through its 50ms watchdog
park cap. If `RIONotify` itself starts failing, the backend degrades
to 1ms polling parks and renames itself `rio-polling` mid-run.
